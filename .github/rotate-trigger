v9.6 preview-incident rotate — re-heal OPENROUTER_API_KEY + APP_ADMIN_PASSWORD after Workers Builds auto-redeploy rebound the wrong admin password (stored length 12 instead of 6) (incident 2026-04-18T12:00:00Z)
